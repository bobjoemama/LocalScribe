[project]
name = "localscribe-python-audit"
version = "0.1.0"
description = "Pinned Python dependency audit toolchain for LocalScribe"
requires-python = ">=3.12,<3.13"
dependencies = [
  "pip-audit==2.10.1",
]

[tool.uv]
package = false
