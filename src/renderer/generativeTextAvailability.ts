/**
 * Build capability contract for renderer surfaces.
 *
 * This is intentionally not phrased as a runtime model-installation result:
 * the current build has no local generative text-model integration to detect.
 */
export const GENERATIVE_TEXT_MODEL_REQUIRED_NOTICE =
  "Unavailable in this build — no local text-model integration";

export const UNAVAILABLE_IN_THIS_BUILD_NOTICE = "Unavailable in this build";
