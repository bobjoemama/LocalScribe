// This committed placeholder is replaced only while Forge prepares a package.
// The generated value is bundled into app.asar, then this exact file is restored.
export const generatedResourceIntegrity = {
  root: "",
  entryCount: 0,
  coveredRoots: [],
  platform: "",
} as const;
